# 아래 버전은 Python 3.12 + Windows 에서 실제로 설치·기동·테스트가 통과한 조합입니다.
# 추측값이 아니라 검증된 값이니 임의로 올리지 마세요. 올릴 땐 pytest 통과 확인 후.
#
# 전이 의존성까지 완전히 똑같이 맞추려면 requirements.lock.txt 를 쓰세요.

# --- 웹 프레임워크 ---
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2

# --- LLM ---
openai==2.45.0
python-dotenv==1.2.2

# --- LLM 추적 (LangSmith — wrap_openai + @traceable) ---
langsmith==0.10.5

# --- 벡터DB (RAG: 컬렉션1 상세페이지 / 컬렉션2 반려사유) ---
chromadb==1.5.9

# --- Agent3 전용: 루프·상태·HITL ---
langgraph==1.2.9

# --- Agent2 전용: 카이제곱·Fisher·비율검정 ---
scipy==1.18.0
statsmodels==0.14.6

# --- 리포팅: 템플릿 렌더링 ---
# app/reporting/pdf_compiler.py 가 모듈 최상단에서 import 한다. tests/test_report.py
# 가 그 모듈을 끌고 오므로, 빼면 pytest 가 수집 단계에서 통째로 멈춘다.
# 순수 파이썬이라 네이티브 의존성이 없어 이미지에 넣어도 부담이 없다.
jinja2==3.1.6

# --- 리포팅: PDF 렌더링 ---
# 🔴 **2026-08-12 이전까지 여기 없었다.** 그때의 근거는 "리포팅 라우터가 501 스텁이라
#    컨테이너가 PDF 경로를 안 탄다"였는데, 그 전제가 이미 깨져 있었다 —
#    `POST /api/v1/replies`(→ cs_reply_service.py `compile_report_to_pdf`)와
#    `python -m app.batch.daily`(→ generate_guideline) 둘 다 **이미지 안에 들어가는
#    app/ 코드**이고 둘 다 PDF 를 만든다. 없는 채로 두면 pip 설치·import 는 다 통과하고
#    (pdf_compiler 가 함수 안에서 import 하므로) **호출 순간에만** ModuleNotFoundError 가
#    나서, 컨테이너를 띄워봐도 그 경로를 안 밟으면 안 잡힌다.
#    → 넣는다. 버전은 requirements.lock 과 같은 값이다.
#
# ⚠️ **pip 만으로는 안 끝난다.** Pango 네이티브 라이브러리와 한글 폰트가 있어야
#    실제로 렌더링된다. Dockerfile 의 apt-get 단계와 **세트로 움직인다** — 한쪽만
#    지우면 다시 호출 시점 폭발이다.
#    로컬 설치 — Windows: GTK3-Runtime Win64(환경변수 추가 옵션 필수.
#                         pdf_compiler.py 가 그 경로를 PATH 에 넣는다)
#                macOS:   brew install pango libffi
#                Debian:  apt install libpango-1.0-0 libpangoft2-1.0-0 \
#                             libharfbuzz-subset0 fonts-noto-cjk
weasyprint==63.1

# --- 리포팅: S3 적재 + Pre-signed URL 서명 ---
# app/reporting/s3_uploader.py 가 **함수 안에서** import 한다(S3 경로를 안 타는 실행에는
# 불필요한 로딩을 피한다). 순수 파이썬이라 네이티브 의존성이 없다.
#
# ⚠️ Pre-signed URL 7일은 SigV4 서명의 상한이라, 클라이언트를 만들 때
#    signature_version="s3v4" 를 명시해야 한다. 구버전 서명은 그만큼 못 버틴다.
boto3==1.43.65

# --- docker kafka 컨테이너 환경 ---
# ⚠️ kafka-python 과 **같은 `kafka` 모듈**을 설치한다. 둘 다 깔면 충돌하므로,
#    기존에 kafka-python 이 있으면 먼저 `pip uninstall kafka-python` 할 것.
kafka-python-ng==2.2.3

# --- RabbitMQ (AI → 메인 이벤트 발행 · docs/mq_events.md) ---
# app/core/mq.py 가 **함수 안에서** import 한다 — payload 조립 테스트가 이 패키지 없이
# 돌아야 하기 때문(브로커 접속 정보가 아직 없다). 발행을 실제로 하려면 필요하다.
aio-pika==9.5.7
# 같은 이유로 직접 적는다 — `topology_config_errors()` 가 채널 오류 3종을 여기서 가져온다.
# aio_pika.exceptions 는 그중 `ChannelAccessRefused` 를 re-export 하지 않는다(9.5.7).
# ⚠️ 정확한 핀(==)을 쓰지 않는 이유는 아래 pamqp 와 같다. 범위는 aio-pika 가 요구하는
#    `aiormq>=6.8,<7.0` 을 그대로 따른다.
aiormq>=6.8,<7
# aio-pika 가 aiormq 를 거쳐 끌어오지만, `_require_ack()` 가 Basic.Ack/Return 을 직접
# 판별하느라 우리 코드가 직접 import 한다 — 직접 쓰는 건 직접 적는다.
# ⚠️ **정확한 핀(==)을 쓰지 않는다.** aiormq 가 `pamqp==3.3.0` 으로 못박고 있어서, 우리가
#    같이 못박으면 pamqp 를 올린 aiormq 패치가 나올 때 resolver 가 **구버전 aiormq 로
#    되돌아간다** — 설치는 성공하므로 조용히 뒤처진다. 실제 버전은 aiormq 가 정하게 둔다.
pamqp>=3.3,<4

# --- 개발/테스트 ---
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1

# --- 린트/포맷 (의존성 없음, 단독 wheel) ---
ruff==0.16.0