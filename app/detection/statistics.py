"""담당: 서영 (Agent2) — 통계 검정 로직.

여기는 순수 함수만. LLM·DB·FastAPI 를 import 하지 않는다.
그래야 fixture 없이도 숫자만 넣어 단위테스트할 수 있다.

TODO(서영): 설계 문서 확정 후 구현.
  검정 방식·임계값·다중비교 보정은 `이상탐지 로직` / `이상탐지 시나리오` 문서를
  기준으로 하되, **개발 착수 시점의 최신본을 다시 확인할 것.**
  (문서가 개정 중이라 여기에 수치·절차를 미리 적어두지 않는다.)

  임계값은 반드시 core/constants.py 를 거친다. 함수 기본인자에 숫자를 박지 말 것.
"""