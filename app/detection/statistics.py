"""담당: 서영 (Agent2) — 통계 검정 로직.

여기는 순수 함수만. LLM·DB·FastAPI 를 import 하지 않는다.
그래야 fixture 없이도 숫자만 넣어 단위테스트할 수 있다.

TODO(서영): 구현.
  - 카이제곱 검정 (채널×상품 분할표)
  - Fisher 정확검정 (기대빈도 5 미만일 때 카이제곱 대신)
  - 이표본 비율검정 (특정 채널 vs 나머지)
  - 표본 < MIN_SAMPLE_SIZE 면 검정 skip (위양성 방지)

주의: p < ALPHA 를 그대로 "이상"으로 쓰면 채널·상품 조합만큼 검정을 반복하게 되어
      다중비교 문제가 생긴다. 위양성함정 케이스가 여기서 걸릴 가능성이 높으니
      보정(Bonferroni/BH) 여부를 정량 실험 때 비교해볼 것.
"""
