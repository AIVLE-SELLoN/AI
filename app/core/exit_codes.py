"""프로세스 종료 코드. **k8s 가 "재시작하면 되나" 를 판단하는 값이다.**

    0  정상 종료
    1  실행 중 오류 — 브로커 접속 실패 등. **재시작하면 나을 수 있다**
    2  설정 문제로 뜨지 못함 — 같은 환경변수를 다시 읽으니 **재시작해도 같다**

**1 과 2 를 뭉개면 안 된다.** 설정 오타를 1 로 보고하면 k8s 는 일시적 오류로 보고 영원히
재시작만 하고(CrashLoopBackOff), 재시작 로그만 쌓이며 **원인 한 줄이 아무 데도 안 남는다.**
실제로 `app/main.py` 가 `LOG_LEVEL=info`(소문자) 하나로 그 상태였다.

core 에 있는 이유는 배포 진입점이 전부 같은 계약을 쓰기 때문이다. 각자 두면 한쪽만 바뀌어도
조용히 갈리고, **갈린 자리는 운영에서만 드러난다** — 로컬에서 종료 코드를 보는 사람은 없다.

**여기에 진입점 목록이나 개수를 적지 말 것.** 적으면 다음 진입점이 생길 때 조용히 거짓이
된다. 대상 집합은
`tests/test_main_entrypoint.py::test_entrypoints_do_not_declare_their_own_exit_codes` 가
소스에서 **유도**하므로, 알고 싶으면 그 테스트를 볼 것. 같은 가드가 진입점이 자기 숫자를
다시 선언하는 것도 잡는다.

`constants.py` 가 아니라 별도 파일인 이유: 그 파일은 스스로를 "정량 실험 노브 + 변경 전
합의 필수" 로 규정하는데 종료 코드는 실험 노브가 아니라 배포 계약이라 그 문구가 거짓이
된다. 옮기고 싶어지면 `constants.py` 머리말을 같이 고칠 것.
"""

EXIT_CONFIG_ERROR = 2
"""설정 문제로 뜨지 못했다. 재시작해도 같으므로 k8s 가 CrashLoopBackOff 로 알린다."""

EXIT_RUNTIME_ERROR = 1
"""실행 중 오류. 재시작하면 나을 수 있다."""
