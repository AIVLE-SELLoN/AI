"""전역 상수.

매직넘버 금지. 정량 실험 때 바꿔가며 돌려야 하는 값들이라 전부 여기 모아둔다.
값을 바꾸면 실험 결과가 통째로 달라지므로 변경 전 팀 합의 필수.

detection 임계값 근거: 이상탐지 로직 V3 §[2]. 파이프라인 [6]~[8]의 상수(원인 일관
기준·알림 억제 등)는 해당 단계 구현 시 추가한다.
"""

from datetime import timedelta, timezone

# --- 시간대 ---

KST = timezone(timedelta(hours=9))
"""날짜 경계의 기준 시간대. **「Raw DB 스키마 확정 (8/7)」 §3 이 KST 로 못박았다.**

UTC 로 자르면 KST 오전 9시 이전 문의가 전날로 밀려서, 매일 도는 배치가 날짜 경계에서
매번 어긋난다. 문서의 쿼리는 `(컬럼 AT TIME ZONE 'Asia/Seoul')::date` 인데 그건
Postgres 문법이라 로컬 sqlite 에 없다 — 그래서 **절단을 파이썬에서 한다**
(`daily._to_kst`). 원문은 오프셋이 붙은 ISO 문자열로 저장되므로(raw_schema 모듈
docstring) 변환에 필요한 정보가 값 안에 다 있다.

⚠️ **core 에 있는 이유: 쓰는 쪽이 갈려 있다.** 탐지 입력 로더(`app/batch/daily.py`)와
탐지 시각 기본값(`app/detection/service.py`)이 같은 경계를 쓰는데, 각자 두면
`timezone(timedelta(hours=9))` 가 여러 벌이 되어 한쪽만 바뀌어도 조용히 갈린다
(`raw_db.py`·`ids.py` 가 core 로 온 것과 같은 사유).
"""

# --- 윈도우 (detection / 서영) — 로직 V3 §137 ---

CURRENT_WINDOW_DAYS = 7
"""현재 윈도우 길이(일). 일별 슬라이딩이라 매일 이 7일 구간으로 재탐지한다."""

PAST_WINDOW_DAYS = 28
"""과거 기준 윈도우 길이(일) = 현재 윈도우 직전 28일.

7일보다 길게 잡는 이유: 기준선은 표본이 커야 흔들리지 않는다. 단, 이 구간에
이미 알림이 나간 날짜가 있으면 제외한다(기준선 오염 방지, §150).
"""

# --- 통계 검정 (detection / 서영) — 로직 V3 §[2] ---

ALPHA = 0.05
"""유의수준. 통계 표준값(관습). Fisher 단측 검정의 raw p 기준."""

MIN_SAMPLE_SIZE = 10
"""[관문①] 현재 윈도우 (상품,채널) 총문의(aspect 무관)가 이 미만이면 판정 보류.

분모가 작으면 비율이 널뛰어서(예: 3건 중 2건=67%) 아예 판정하지 않는다.
보류는 채널 단위 — 그 채널의 모든 aspect가 함께 빠진다.
"""

MIN_DELTA = 0.03
"""[관문③] 최소 상승폭 3%p. p<ALPHA 발화분에 delta>=MIN_DELTA 를 AND 로 겹친다.

다중검정 오탐 게이트가 아니다(부록 A: 오탐 감소 0%). 배경 볼륨에서 상승폭
표준편차가 4.10%p라 3%p는 0.73 SD, 노이즈 범위 안이라 non-binding.
실제 역할: N이 클 때 통계적으론 유의하나 실무적으로 무의미한 미미한 상승(+0.5%p 등)
차단. 다중검정 방어는 BH-FDR(관문②) 담당.
평가셋 스윕으로 캘리브레이션할 값이라 여기 상수로 둔다.
"""

BH_FDR_Q = 0.05
"""[관문②] 상품별 BH-FDR의 목표 FDR 수준.

독립 또는 양의 의존 등 BH 가정 아래에서 **상품별 기각 가설 집합의 기대 거짓발견률**을
제어한다. 한 번의 실행에서 최종 발행 알림의 헛알림률이 항상 5% 이하라는 뜻은 아니다.
결합·억제·채점 정책을 거친 데모 헛알림률은 별도 실측 지표다.

보정 없으면 정상 상품 1개가 하루 54.8% 확률로 오탐(부록 A) → BH 필수.

🔴 **값은 불변(0.05). 바뀐 것은 family 다** (2026-08-13):
    윈도우 전체(m≈1,464) 1개 family → **상품별 family** (`statistics.decide_fires`).
    BH 는 step-up 이라 임계가 family 안의 발견 수에 비례해 오르는데, 전체를 한 family 로
    두면 평가 배치(참양성 41개)와 일별 운영 배치(하루 1~3개)의 실효 임계가 36배 벌어져
    **채점은 통과인데 운영·데모에선 안 뜨는** 상태가 됐다. 사유·실측은 `decide_fires`
    docstring 참고.

⚠️ **소비처 영향**: 대시보드 '유의 ✓' 배지(`stats.bh_significant`, 탐지 스키마 §3.3)가
   더 자주 뜬다. **스키마 변경은 아니다.** Agent3·가이드라인 호출도 늘어난다.
"""

# --- 원인 분류 일관성 (detection / 서영) — 로직 V3 §[6] ---

CONSISTENT_RATIO = 0.50
"""[6] 최다 원인이 이 비율 이상이어야 '원인 일관'. 로직 §[6] judge_cause.

미달이면 원인이 흩어진 것으로 보고 특정하지 않는다 → [7]에서 확신도 낮음 경로.
"""

CONSISTENT_COUNT = 5
"""[6] 그리고 최다 원인 건수가 이 이상이어야 '원인 일관'. 저건수 우연 일치 방지."""

# --- 알림 억제 (detection / 서영) — 로직 V3 §6 알림 운영 규칙 ---

RENOTIFY_BLOCK_DAYS = 7
"""같은 (상품, aspect, 채널) 조합의 재알림 금지 기간(일). 로직 §6.

일별 슬라이딩이라 같은 이상이 윈도우에서 빠질 때까지 매일 재탐지된다. 억제가 없으면
셀러에게 같은 알림이 7일 내내 간다. [1]의 기준선 오염 방지와 짝을 이루는 규칙이다.

🔴 **불변식: RENOTIFY_BLOCK_DAYS <= CURRENT_WINDOW_DAYS. 지금 여유가 0 이다.**
   (지인님 지적, 2026-08-05)

   억제된 날(d2~d7)은 알림이 발행되지 않아 `prior_alerts` 에 안 남는다. 그런데도
   기준선에서 빠지는 이유는, 억제가 풀린 뒤 d8 에 나가는 알림의 윈도우가 d2~d8 이라
   그 구간을 **덮어주기** 때문이다(`service._alert_days` 가 window_start~window_end 를
   제외한다).

   이 값을 CURRENT_WINDOW_DAYS 보다 키우면 두 알림 윈도우 사이에 틈이 생기고, 그
   틈의 이상 구간이 과거 기준선에 섞여 **'새로운 평소'로 굳는다 → 알림이 스스로 꺼진다.**

       block=7   덮이지 않는 날 0개
       block=8   3개
       block=14  21개

   → 늘리려면 CURRENT_WINDOW_DAYS 를 함께 늘리거나, 억제된 날짜를 따로 기록해
     `_alert_days` 에 넘겨야 한다. tests/test_pipeline.py 가 이 불변식을 고정한다.
"""

RENOTIFY_DELTA_JUMP = 0.05
"""억제 기간 중이라도 부정률이 직전 알림 대비 이만큼(+5%p) 더 오르면 갱신 알림 허용.

로직 §6. 갱신은 새 alert_id 를 발급하고 updates_alert_id 에 원본을 담는다.
"""

# --- LLM 공통 ---

MAX_RETRY = 2
"""LLM 호출/파싱 실패 시 재시도 횟수. 총 시도 = 1 + MAX_RETRY."""

# --- 인용 검증 (recommendation / 지인) — 개선안 로직 §2·§4-3 ---

GROUNDING_SIMILARITY_THRESHOLD = 0.8
"""인용문 grounding 유사도 임계값. 정규화 후 완전 부분일치 실패 시의 완화 기준.

완전일치는 조사 하나만 달라도 깨지고(오탐), 임계값을 너무 낮추면 환각을 통과시킨다.
quote와 source_text 정규화 문자열 사이 최장 연속 일치 구간 길이 / quote 길이로 계산.
평가셋으로 캘리브레이션할 값이라 상수로 둔다.
"""

SIMILAR_CASE_TOP_N = 3
"""컬렉션2(과거·반려 사례) 유사도 조회 상위 건수. 개선안 로직 §4-2."""

CS_QUOTE_TOP_N = 5
"""image_guide 근거로 프롬프트에 싣는 CS 원문 건수 상한. 개선안 로직 §4-3.

`evidence.inquiry_ids` 는 알림 1건당 `root_cause.total` 건까지 오는데, 전부 실으면
프롬프트가 길어지는 것보다 **인용 대상이 흩어지는 게 문제다** — LLM 이 어느 문의를
인용했는지 흐려지고 citations 대조도 그만큼 느슨해진다. 앞에서부터 자른다(순서는
`evidence.inquiry_ids` 를 따르므로 탐지가 정한 우선순위가 유지된다).
"""

# --- 문서 생성 (reporting / 용준) — 문서 생성 스키마(확정) §1·§3·§4 ---

MONTHLY_ASPECT_COUNT = 3
"""월간 리포트가 다루는 aspect 개수(색상·사이즈·소재) 고정 길이. 스키마 §1-1.

aspect_distributions·sentiment_drifts·aspect_summaries 세 배열의 길이가 전부 이 값이며,
서로 aspect 집합이 같아야 한다. CS 탐지용 6종(Aspect enum 전체)과 달리 3종으로 제한된다.
"""

MAX_CHANNEL_PAIRS = 3
"""월간 채널쌍 최대 개수 = C(채널 3종, 2) = 3.

⚠️ 채널이 늘면 이 값도 함께 키워야 한다. 검증기가 "입력 pairs 전부에 분석이 있을 것" 을
   요구하므로, 입력 쌍이 이 상한을 넘으면 LLM 이 아무리 잘 써도 스키마가 잘라내 **영구
   FAILED_VALIDATION** 이 된다(재시도로도 못 빠져나온다).
"""

DRIFT_RISK_THRESHOLD = 0.03
"""sentiment_drifts[].status 가 RISK 가 되는 ΔP_neg 하한. 스키마 §1-1.

경고 박스 스타일을 가르는 값이라 바꾸면 리포트 톤이 통째로 달라진다.
detection 의 MIN_DELTA 와 수치는 같지만 다른 계약(월간 리포트)의 값이라 별도로 둔다 —
한쪽을 조정할 때 다른 쪽이 딸려가면 안 된다.
"""

RATIO_SUM_TOLERANCE = 0.005
"""비율 합 검증 허용 오차. 스키마 §1-1 "세 비율 합 = 1.00 (±0.005)".

positive+neutral+negative 합, CS 통계의 delta = cur_rate − past_rate 검증에 함께 쓴다.
부동소수 반올림 흡수용이라 이보다 키우면 집계 버그를 놓친다.
"""

MAX_PDF_SIZE_BYTES = 10_485_760
"""생성 PDF 용량 상한(10MB). 스키마 §3-1.

초과하면 S3 업로드·메일 발송 트랜잭션 이전에 FAILED_SIZE_EXCEEDED 로 차단한다(§4-3).
"""

# --- S3 보존 정책 (2026-08-03 확정) — S3 Lifecycle 규칙과 **반드시 같은 값**이어야 한다 ---
#
# 코드가 계산하는 `object_expires_at` 은 이 상수에서 나온다. 버킷의 Lifecycle 설정을
# 바꾸면서 여기를 안 고치면, 실제로는 삭제된 파일을 "아직 받을 수 있다"고 안내하게 된다.

GUIDELINE_RETENTION_HOURS = 7 * 24
"""CS 가이드라인 PDF 자동 삭제까지의 시간 = **7일** (2026-08-06 확정).

⚠️ 24시간이었다가 늘렸다. 화면 플로우에 **운영 MD 승인이라는 사람 단계**가 들어 있어서다:
   AI 답변 초안 → 운영 MD 승인 → 답변 발송 → 처리 완료.
   메일은 승인 **뒤에** 나가는데, 승인이 하루를 넘기면 객체가 이미 지워져 발송할 것이
   없어진다. 승인 대기 중에 산출물이 사라지는 구조는 성립하지 않는다.
   (인프라 문서가 제시한 "1일(최대 7일 이내)" 범위의 상한이다.)

출력 데이터는 DB 에 남으므로 만료 후에는 재생성할 수 있다. 다만 화면의 '재시도' 는
재생성이 아니라 **S3 에 있는 문서를 다시 불러오는** 동작이라, 만료된 뒤에는 그 버튼으로
복구되지 않는다 — 재생성 요청이 따로 와야 한다."""

PRESIGNED_URL_TTL_HOURS = 7 * 24
"""Pre-signed URL 만료까지의 시간 — **문서 종류 무관 7일 고정** (인프라 §5, 2026-08-06).

AI 노드가 **업로드 시점에 최초 1회** 발급한다. 메인 서버는 그 링크가 유효한 동안
**재사용**하고, 만료됐을 때만 `s3_full_key` 로 재발급한다.
(예전에는 24시간 + "발송마다 재발급"이었다. 인프라 문서가 7일 + 재사용으로 확정했다.)

7일은 SigV4 서명의 상한이기도 하다 — 더 길게는 만들 수 없다.

⚠️ 객체보다 링크가 오래 살 수는 없다. CS 가이드라인은 객체 수명도 7일이라 두 값이 같고,
   월간 리포트는 객체 6개월 / 링크 7일이라 링크가 먼저 만료된다."""

MONTHLY_RETENTION_DAYS = 180
"""월간 리포트 PDF 자동 삭제까지의 일수(6개월).

⚠️ 월간은 원본 데이터를 보관하지 않는다(PDF 가 유일 산출물). 즉 **만료 = 영구 소실**이라
   재생성이 불가능하다.
   6개월 경과 리포트는 조회하지 않기로 확정했으므로(2026-08-03) 아카이빙은 두지 않는다.
   이 값을 **줄이면** 아직 볼 수 있어야 할 리포트가 사라지므로 변경 전 합의 필수.
"""

SEVERITY_STAGE_LABEL: dict[str, str] = {
    "SAFE": "안정 단계",
    "CAUTION": "주의 단계",
    "CRISIS": "위험 단계",
}
"""severity → cause_title 에 반드시 포함돼야 하는 단계 라벨. 스키마 §1-2.

다른 단계의 라벨이 섞이면 반려한다. 키는 Severity enum 의 value 와 1:1.
"""

HOLD_INSUFFICIENT_DATA_NOTICE = (
    "해당 상품의 월간 CS 표본 수는 부족으로 인하여 보고서 생성이 보류되었습니다. "
    "데이터가 누적되면 분석이 재개됩니다."
)
"""HOLD_INSUFFICIENT_DATA 콜백의 고정 안내 문구. 스키마 §4-3.

LLM 이 생성하는 문장이 아니라 문서에 못박힌 고정 문자열이다 — 임의로 바꾸지 말 것.
"""

MIN_VOC_COUNT_FOR_REPORT = 10
"""월간 보고서 생성 최소 표본. total_voc_count 가 이 미만이면 LLM 추론을 아예 돌리지 않고
HOLD_INSUFFICIENT_DATA 로 보류한다(스키마 §1-1·§4-3). detection 의 MIN_SAMPLE_SIZE 와
수치는 같지만 다른 계약이라 별도로 둔다.
"""

NOTICE_MAX_CHARS = 255
"""콜백 `notice_message` **전체**의 글자 수 상한. `_build_excluded_notice` 가 보장한다.

⚠️ 상한을 **조립된 최종 문자열**에 건다. 구절(보류/실패)마다 따로 예산을 주면 상한이 안
   지켜진다 — 구절 수와 고정 문구 길이만큼 천장이 같이 올라가기 때문이다. 구절당 70자로
   주던 때의 실측(2026-08-10 리뷰):

       보류/실패      이름 38자   이름 82자
        42 / 42         226        252    ← 이 조합만 보고 "255 안" 이라고 적었다
       150 / 150        230      **256**  ← 초과
       천장(자릿수 최대)  232      **260**  ← 초과

   두 축이 예산 밖에 있었다:
     · **긴 이름일수록 예산을 더 쓴다.** 38자 이름은 라벨이 45자라 두 번째가 안 들어가
       45자에서 멈추지만, 70자를 넘는 이름은 정확히 70자로 잘려 예산을 꽉 채운다.
       쿠팡·네이버 노출명은 SEO 키워드가 붙어 70자를 넘는 게 흔하다.
     · **개수 자릿수가 4곳에 들어간다** — 양쪽 구절의 "상품 N개" 와 "외 N개".
       2자리→3자리면 +4자다. 카탈로그가 504개(`input_detail_fields.csv`)라 보류 3자리는
       정상 범위다(VOC 10건 미만이 보류니까).

⚠️ 개수가 아니라 **길이**로 자른다. 개수로 자르면 상품명이 길 때 넘친다 —
   `_fetch_product_names()` 가 `products.channel_product_name`(커머스 노출명)을 자르지
   않고 그대로 싣는다. 개수 상한(5개)만 걸었을 때 실측(2026-08-09):
       목 데이터 이름(7자)  보류42+실패42 = 223자   ← 로컬에서는 안 밟힌다
       실제 노출명(38자)    보류42+실패42 = **368자**

접어도 되는 근거: 보류 상세는 **합본 PDF 의 보류 페이지**에 상품마다 있다(2026-08-09).
⚠️ 다만 그 근거는 **보류에만** 성립한다 — `pdf_compiler` 는 `held` 만 페이지로 만들고
   `failed_products` 는 지면에 안 남는다. 실패가 이 상한을 넘으면 이름이 셀러 쪽에는
   어디에도 안 남지만, 배치 요약 JSON 에 전체 목록이 남아 "운영자가 확인 중" 은 지켜진다.
   실패가 그만큼 많으면 그건 배치 장애라 안내 문구로 풀 문제가 아니다.

⚠️ **백엔드 컬럼의 실제 제한을 아직 확인 못 했다.** 255 는 VARCHAR(255) 를 가정한 값이다.
   확인할 때 **글자 수인지 바이트인지**도 같이 물어야 한다 — 255자가 전부 한글이면 UTF-8
   로 약 700바이트라, 컬럼이 바이트 기준이면 이 값이 3배 가까이 틀린 셈이 된다.
"""

HOLD_IN_BOOK_NOTICE = (
    f"해당 상품은 월간 VOC 가 {MIN_VOC_COUNT_FOR_REPORT}건 미만이라 "
    "리포트 생성이 보류되었습니다."
)
"""합본 PDF 의 **보류 상품 페이지**에 찍는 문구 (2026-08-09).

위 `HOLD_INSUFFICIENT_DATA_NOTICE` 와 쓰임이 다르다:
  HOLD_INSUFFICIENT_DATA_NOTICE  콜백(`notice_message`)용 — 스키마 §4-3 이 못박은 고정 문자열
  HOLD_IN_BOOK_NOTICE            PDF 지면용 — 보류 사유를 문서 안에서 바로 읽게 한다

예전에는 보류 상품이 합본에서 통째로 빠지고 콜백 문구로만 나갔다. 그러면 **PDF 만 받아
보는 사람은 자기 상품이 왜 없는지 알 방법이 없다** — 목차도 표지도 없어서 "빠졌다"는
사실조차 안 보인다. 그래서 보류 상품도 페이지를 만들어 사유를 적는다.

⚠️ 기준은 **미만**(`< 10`)이다. '10건 이하'로 쓰면 정확히 10건인 상품은 리포트가
   생성되는데도 보류라고 안내하게 된다.
"""

# --- 채널 분열 판정 (reporting / 용준) — 문서 생성 스키마 §4-2 ---

JSD_DELTA_MIN = 0.10
"""δ_min (bits). excess = jsd_score − jsd_baseline 를 이 값과 비교해 단계를 가른다.

excess < δ_min 또는 미유의 → SAFE / δ_min ≤ excess < 2δ_min 이고 유의 → CAUTION /
excess ≥ 2δ_min 이고 유의 → CRISIS. 게이지 문구 단계를 정하는 값이라 변경 시 합의 필요.
"""

JSD_GATE_MIN_TOTAL = 30
"""[게이트] 두 채널 부정 문서 합 N 의 하한. min(n_A, n_B) ≥ 1 과 AND 로 묶인다.

미충족이면 판정 6개 값을 전부 null 로 두고 hold_reason 을 세팅한다(반쪽 상태 금지).
"""

PERMUTATION_B = 10_000
"""순열검정 반복 횟수 B. p값 해상도가 1/B 라 이보다 줄이면 BH-FDR 이 무뎌진다."""

# --- 출력 검증 (reporting / 용준) — 문서 생성 스키마 §4-4 ---

FORBIDDEN_METRIC_EXPRESSIONS = (
    "p-value",
    "p값",
    "p 값",
    "FDR",
    "유의확률",
)
"""출력 텍스트 금지 표현. 통계 용어가 셀러용 문서에 노출되면 안 된다(§4-4).

`p = 0.0x` 형태는 정규식으로 따로 잡는다(FORBIDDEN_P_VALUE_PATTERN).
"""

FORBIDDEN_P_VALUE_PATTERN = r"p\s*[=<>≤≥]\s*0?\.\d+"
"""`p = 0.03`, `p<0.05` 같은 p값 노출 패턴. FORBIDDEN_METRIC_EXPRESSIONS 와 함께 검사한다."""

FACTCHECK_NUMBER_TOLERANCE = 0.5
"""수치 팩트체크 허용 오차. 출력 문장의 `13%` / `8%p` / `450건` 이 입력 수치와
이 값 이내로 맞아야 통과한다(반올림 표기 흡수용). 단위: 표기된 수치 그대로(%, %p, 건).
"""

FACTCHECK_SCORE_TOLERANCE = 0.005
"""단위 없는 소수(JSD 점수 등)의 팩트체크 허용 오차.

`0.54` 를 %/건과 같은 ±0.5 로 재면 전혀 다른 점수(0.18)까지 통과해버려서 따로 둔다.
소수 둘째 자리 반올림만 흡수하는 폭이다.
"""

ROOT_CAUSE_UNSPECIFIED_TEXT = "원인 미특정"
"""root_cause 가 null 일 때 root_cause_summary 에 반드시 포함돼야 하는 대체 문구(§2-2).

⚠️ 원본 문서의 해당 칸 문자열이 캡처에서 판독되지 않아 임시로 정한 값이다 — 확정 문구
확인되면 교체할 것.
"""

# --- 벡터DB 컬렉션 이름 ---

COLLECTION_DETAIL_PAGES = "detail_pages"
"""컬렉션1 — 상세페이지. 개선안 생성의 인용 근거."""

COLLECTION_REJECTION_REASONS = "rejection_reasons"
"""컬렉션2 — 반려 사유. 다음 생성 시 참고 (B5 반려 → 적재)."""

EMBEDDING_MODEL = "text-embedding-3-small"
"""두 컬렉션 공통 임베딩 모델(다국어).

⚠️ 이 값을 바꾸면 **두 컬렉션 다 재시딩해야 한다** — Chroma 가 컬렉션 설정에 임베딩
함수를 저장해두고 불일치 시 열기를 거부한다(vectordb._get_collection)."""
