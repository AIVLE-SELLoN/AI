---

## 추가 측정 — 회차 독립 3회 실행 (2026-08-10)

위에서 "수작업으로 커버리지를 채워 84%"라고 적은 값은 **폐기합니다.**
이 PR의 수정(`0f3e76e`)을 얹고 **빈 캐시에서 `--runs 3` 을 한 번 실행**해 다시 쟀습니다.

```
커밋   0f3e76e  (회차 내 재시도 — 배치 + per_item 양쪽)
지문   classify_aspect_v5-af48384c_classify_sentiment_v4-ef7a6c8d-d1fb05ed9
로그   eval/results/exp2_run_independence_0f3e76e.log  (771ef17)
명령   python eval/run_pipeline_eval.py --runs 3
```

### 회차별

| 회차 | 최초 대상 | 회차 내 재시도 | 최종 무응답 | 제외 슬롯 | 탐지율 |
| --- | --- | --- | --- | --- | --- |
| 1 | 12,410 (캐시 0) | 33건 (1/2) | **0** | **0** | 80.0% |
| 2 | 12,410 (캐시 0) | 43건 (1/2) | **0** | **0** | 84.0% |
| 3 | 12,410 (캐시 0) | 7건 (1/2) | **0** | **0** | 72.0% |

세 회차 모두 **12,410 / 12,410 으로 닫혔습니다.** 커버리지 미달 0건, 최종 무응답 0건.
재시도는 전부 1회로 끝나 예산(`RETRY_WITHIN_RUN = 2`) 안에 들어왔습니다.

### 결과

| 지표 | ① oracle | ② 실제분류 | 하락폭 |
| --- | --- | --- | --- |
| 탐지율(recall) | 100.0% | **78.7% ±6.0%** | −21.3% |
| 오탐률(FPR) | 0.0% | **0.0% ±0.0%** | ±0.0% |
| verdict 정확도 | 100.0% | 83.8% ±4.5% | −16.2% |
| is_biased 정확도 | 100.0% | 78.7% ±6.0% | −21.3% |
| main_aspect 정확도 | 100.0% | 77.3% ±6.0% | −22.7% |
| 편중 채널 정확도 | 100.0% | 91.9% ±2.3% | −8.1% |

### 왜 84%가 아니라 78.7%인가

**84%는 세 회차 중 하나였습니다** (회차 2). 수작업 충전판이 우연히 그 회차와 같은 값을
냈던 것이고, 독립 시행에서는 72~84% 로 벌어집니다.

더 중요한 건 **±0.0% → ±6.0%** 입니다. 이전 ±0.0% 는 "안정적"이라는 뜻이 아니라,
회차별 커버리지를 사람이 맞춰놓아 **흔들림이 가려진 상태**였습니다. 6%p 폭이 실제 값입니다.

따라서 프롬프트 개선 효과는 **56% → 84%** 가 아니라 **56% → 78.7% ±6.0%** 로 정정합니다.

### 이 PR이 고친 것과의 연결

수정 전에는 회차가 무응답을 남긴 채 닫히고, 그 무응답이 **다음 실행**에서 채워졌습니다.
그래서 같은 회차 1이 `--runs 1` 에서 60.0%(7슬롯 제외), `--runs 2` 를 돌리면 84.0% 가
됐습니다 — 모델이 아니라 채점 대상이 달라진 것입니다.

이번 실행에서는 세 회차의 채점 대상이 완전히 동일하므로, **±6.0% 는 커버리지 비대칭이
아니라 LLM 흔들림을 잰 값**입니다.

`tests/test_run_within_retry.py` 5개가 배치·per_item 양쪽의 재시도 성공/예산 소진/예산
상한을 고정합니다 (리뷰 420건은 프롬프트2 에 배치 조립기가 없어 항상 per_item 입니다).
