"""담당: 용준 — 메일 발송.

TODO(용준): 구현.
  - send_mail(to, subject, body) -> None

주의:
  - SMTP 계정·비밀번호는 절대 하드코딩 금지. config.py 에 환경변수로 추가하고
    .env.example 에도 키 이름을 넣어두세요 (값은 비워서).
  - config.py 는 core/ 는 아니지만 전원이 쓰는 파일이라, 항목 추가 시 팀 채팅 공지.
  - 개발 중에는 실제 발송 대신 로그로 대체하는 스위치를 두는 편이 안전합니다.
    (테스트 돌리다 실제 메일이 나가는 사고 방지)
"""
