"""실험⑥ 프롬프트3 평가 — 원인 분류 정확도 ([6] 원인 진단).

번호 근거: ①~④(mock 정의서 §579) + ⑤ RAG 베이스라인 비교(개선안 로직 §182, 지인).
   ⑤가 이미 RAG 실험이라 원인분류는 ⑥. 지인 승인 완료(2026-07-23).
   → mock 정의서 "4종"·기술 정리 "①~④"는 노션에서 6종으로 갱신 필요(타 담당 문서).

무엇을 재나: 발화 건의 원인 라벨(예: 사진_색감_오차)이 정답과 맞나 + '원인 특정'
            판정(최다 ≥50% AND ≥5건)이 골든과 일치하나
어떻게:     LLM(프롬프트3)으로 문의 텍스트를 원인 분류 → config_anomaly 의
            cause_distribution(정답 분포)과 대조
비용:       ~$20 (추정) — 케이스당 ~30건 × 채점 케이스. 예산 재산정 필요.

⚠️ 이 실험은 detection_confidence(신뢰도 출력)와 다르다.
   - 여기(실험⑤): 원인 라벨이 '맞았나'를 채점 = 성능 평가
   - decide_confidence(): 원인이 '일관됐나'를 재료로 신뢰도를 정함 = 런타임 출력
   섞지 말 것.

⚠️ 스코프: 색상/사이즈/소재 계열 원인만. **파손·오배송은 제외**(이상탐지 로직에서
   원인 후보 미정의·개선안 없음으로 root_cause="—" 정정됨).

⚠️ 시험지 유출 금지. 프롬프트3의 few-shot 예시는 평가셋 밖에서 사람이 작성한다.
   생성 프롬프트(작가)와 원인분류 프롬프트(응시자)를 분리 관리할 것.

실행:
    python eval/run_cause_eval.py

TODO: 데이터·스키마 확정 후 구현.
  - data/config/config_anomaly.csv 의 cause_distribution 을 정답으로 로딩
  - 평가셋 문의 텍스트 → 프롬프트3 원인분류 → 최다 원인·비율 산출
  - 라벨 정확도 + '원인 특정' 판정(≥50% & ≥5건) 일치율을 나눠서 리포트
  - 프롬프트 버전을 인자로 받게 할 것 (v1 vs v2 비교가 정량 실험의 핵심)
  - 결과에 프롬프트 버전·모델명·일시 기록
"""


def main() -> None:
    raise NotImplementedError("데이터·스키마 확정 후 구현")


if __name__ == "__main__":
    main()
